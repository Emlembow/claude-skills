{
  "name": "claude-skills",
  "version": "1.0.0",
  "private": true,
  "description": "Marketplace-ready Claude skills collection.",
  "scripts": {
    "skills:list": "npx --yes skills@latest add . --list",
    "validate:marketplace": "claude plugin validate .",
    "validate:plugins": "for d in skills/*; do if [ -d \"$d/.claude-plugin\" ]; then claude plugin validate \"$d\"; fi; done",
    "validate": "npm run skills:list && npm run validate:marketplace && npm run validate:plugins"
  }
}
